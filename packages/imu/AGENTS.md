# IMU Package

Read [README.md](./README.md) first.

Canonical `InertialMeasurementUnitSample` type shared across all IMU sources: CoreMotion (iPhone/Watch), WHOOP BLE, dofek-zepp (ZeppOS), and the server pipeline. All consumers import directly from `@dofek/imu`.
