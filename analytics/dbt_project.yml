name: "dofek_analytics"
version: "1.0.0"
config-version: 2

profile: "dofek"

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dofek_analytics:
    +materialized: view
