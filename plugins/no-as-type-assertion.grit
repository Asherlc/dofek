TsAsExpression(ty=$y) as $assertion where {
  $text = text($y),
  $text <: not r"^const$",
  register_diagnostic(
    span = $assertion,
    message = "Type assertion with 'as' is banned. Use Zod parsing, type guards, generics, or 'satisfies' instead.",
    severity = "error"
  )
}
