services:
  peerdb-catalog:
    image: postgres:18-alpine
    environment:
      PGUSER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-health}
      POSTGRES_DB: postgres
      POSTGRES_INITDB_ARGS: --locale=C.UTF-8
    volumes:
      - peerdb_catalog_data:/var/lib/postgresql
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d postgres"]
      interval: 10s
      timeout: 30s
      retries: 5
      start_period: 60s

  peerdb-temporal:
    image: temporalio/auto-setup:1.29
    environment:
      DB: postgres12
      DB_PORT: "5432"
      POSTGRES_USER: postgres
      POSTGRES_PWD: ${POSTGRES_PASSWORD:-health}
      POSTGRES_SEEDS: peerdb-catalog
      DYNAMIC_CONFIG_FILE_PATH: config/dynamicconfig/development-sql.yaml
      TEMPORAL_BROADCAST_HOST: "127.0.0.1"
    restart: unless-stopped
    depends_on:
      peerdb-catalog:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "tctl", "--address", "127.0.0.1:7233", "workflow", "list"]
      interval: 10s
      timeout: 10s
      retries: 12
      start_period: 60s

  peerdb-temporal-admin-tools:
    image: temporalio/admin-tools:1.29
    environment:
      TEMPORAL_ADDRESS: peerdb-temporal:7233
      TEMPORAL_CLI_ADDRESS: peerdb-temporal:7233
      TEMPORAL_CLI_SHOW_STACKS: "1"

  peerdb-minio:
    image: minio/minio:latest@sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e
    environment:
      MINIO_ROOT_USER: peerdb
      MINIO_ROOT_PASSWORD: ${POSTGRES_PASSWORD:-health}
      PEERDB_CLICKHOUSE_AWS_S3_BUCKET_NAME: peerdbbucket
    volumes:
      - peerdb_minio_data:/data
    restart: unless-stopped
    entrypoint: >
      /bin/sh -euc "
      minio server /data --console-address=:36987 &
      attempts=0;
      until mc alias set peerdb-minio http://peerdb-minio:9000 $$MINIO_ROOT_USER $$MINIO_ROOT_PASSWORD; do
        attempts=$$((attempts + 1));
        if [ $$attempts -ge 30 ]; then
          echo peerdb-minio did not become ready in time >&2;
          exit 1;
        fi;
        sleep 1;
      done;
      mc mb --ignore-existing peerdb-minio/$$PEERDB_CLICKHOUSE_AWS_S3_BUCKET_NAME;
      wait
      "

  peerdb-flow-api:
    image: ghcr.io/peerdb-io/flow-api:stable-v0.36.18
    environment: &peerdb-flow-environment
      PEERDB_CATALOG_HOST: peerdb-catalog
      PEERDB_CATALOG_PORT: "5432"
      PEERDB_CATALOG_USER: postgres
      PEERDB_CATALOG_PASSWORD: ${POSTGRES_PASSWORD:-health}
      PEERDB_CATALOG_DATABASE: postgres
      TEMPORAL_HOST_PORT: peerdb-temporal:7233
      TEMPORAL_CLIENT_CERT: ""
      TEMPORAL_CLIENT_KEY: ""
      PEERDB_TEMPORAL_NAMESPACE: default
      PEERDB_ALLOWED_TARGETS: ""
      PEERDB_CLICKHOUSE_AWS_CREDENTIALS_AWS_ACCESS_KEY_ID: peerdb
      PEERDB_CLICKHOUSE_AWS_CREDENTIALS_AWS_SECRET_ACCESS_KEY: ${POSTGRES_PASSWORD:-health}
      PEERDB_CLICKHOUSE_AWS_CREDENTIALS_AWS_REGION: us-east-1
      PEERDB_CLICKHOUSE_AWS_CREDENTIALS_AWS_ENDPOINT_URL_S3: http://peerdb-minio:9000
      PEERDB_CLICKHOUSE_AWS_S3_BUCKET_NAME: peerdbbucket
      AWS_ACCESS_KEY_ID: ""
      AWS_SECRET_ACCESS_KEY: ""
      AWS_REGION: ""
      AWS_ENDPOINT: ""
    restart: unless-stopped
    depends_on:
      peerdb-catalog:
        condition: service_healthy
      peerdb-temporal:
        condition: service_started
      peerdb-minio:
        condition: service_started

  peerdb-flow-snapshot-worker:
    image: ghcr.io/peerdb-io/flow-snapshot-worker:stable-v0.36.18
    environment: *peerdb-flow-environment
    restart: unless-stopped
    depends_on:
      peerdb-flow-api:
        condition: service_started

  peerdb-flow-worker:
    image: ghcr.io/peerdb-io/flow-worker:stable-v0.36.18
    environment: *peerdb-flow-environment
    restart: unless-stopped
    depends_on:
      peerdb-flow-api:
        condition: service_started

  peerdb:
    image: ghcr.io/peerdb-io/peerdb-server:stable-v0.36.18
    stop_signal: SIGINT
    environment:
      PEERDB_CATALOG_HOST: peerdb-catalog
      PEERDB_CATALOG_PORT: "5432"
      PEERDB_CATALOG_USER: postgres
      PEERDB_CATALOG_PASSWORD: ${POSTGRES_PASSWORD:-health}
      PEERDB_CATALOG_DATABASE: postgres
      PEERDB_PASSWORD: ${POSTGRES_PASSWORD:-health}
      PEERDB_FLOW_SERVER_ADDRESS: grpc://peerdb-flow-api:8112
      RUST_LOG: info
      RUST_BACKTRACE: "1"
    restart: unless-stopped
    ports:
      - "127.0.0.1:9900:9900"
    depends_on:
      peerdb-catalog:
        condition: service_healthy
      peerdb-flow-api:
        condition: service_started

volumes:
  peerdb_catalog_data:
  peerdb_minio_data:
