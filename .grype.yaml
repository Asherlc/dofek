ignore:
  # Go stdlib CVEs in vendored static binaries (Infisical CLI, Docker CLI).
  # Fix requires upstream projects to rebuild with newer Go — not in our control.
  - vulnerability: CVE-2025-68121
  - vulnerability: CVE-2025-22871
