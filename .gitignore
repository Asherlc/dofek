node_modules/
dist/
.claude/
# .env is SOPS-encrypted and safe to commit
# .env.plain is the decrypted version — never commit
.env.plain
*.log
# Personal supplement stack — differs per user
src/supplements.config.ts
packages/ios/ios/
