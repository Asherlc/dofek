node_modules/
dist/
# OTA code signing keys (secrets — only certificate.pem is committed)
packages/mobile/certs/private-key.pem
packages/mobile/certs/public-key.pem
# Track .claude/skills (custom skills), ignore everything else
.claude/*
!.claude/skills/
.context/
.DS_Store
*.swp
*.swo
.idea/
.vscode/
.env.local
.env.*.local
# .env is committed non-secret config; .env.local is gitignored for local overrides
.env.local
.env.*.local
*.log
# Personal supplement stack — differs per user
src/supplements.config.ts
# Storybook build output
**/storybook-static/
# Stryker mutation testing
reports/
.stryker-tmp/
# Cypress
cypress/videos/
cypress/screenshots/
cypress/downloads/
# Code coverage
coverage/
# Playwright test results
test-results/
# Swift Package Manager
.build/
.swiftpm/
# Muter mutation testing
muter_logs/
# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.coverage
# Terraform plan/state artifacts (can contain secrets)
deploy/tfplan*
deploy/*.tfplan
deploy/*.tfstate.*
# iOS — generated by expo prebuild (continuous native generation)
packages/mobile/ios/
packages/mobile/sentry.options.json
# Downloaded Sentry XCFramework and private headers (see scripts/download-sentry-xcframework.sh)
packages/mobile/native/sentry-pod/Sentry.xcframework/
packages/mobile/native/sentry-pod/Sources/

packages/ios/ios/Dofek.xcodeproj/project.xcworkspace/
packages/ios/ios/Dofek.xcworkspace/
packages/ios/ios/Pods
packages/ios/ios/build

# Gemini CLI
.gemini/
.worktrees/
