# Kaya climbing TDD progress

Disposable progress log for executing `docs/superpowers/plans/2026-07-09-kaya-climbing-grade-progression-tdd.md`.

## 2026-07-09

- Reviewed the plan and applicable TDD/executing-plan skills.
- Current branch is `Asherlc/rock-climbing-tab`; not starting from `main`.
- Starting with Task 1: shared climbing grade utility tests.
- Task 1 red test added at `packages/training/src/climbing-grades.test.ts`.
- Plan command `pnpm test -- run ...` ran the full suite in this repo and hit unrelated Apple Health integration container failure; targeted command is `rtk pnpm exec vitest run --project unit <file>`.
- Targeted red verification failed as expected: `Cannot find module './climbing-grades.ts'`.
- Task 2 implemented `packages/training/src/climbing-grades.ts` and exported `./climbing-grades` from `@dofek/training`.
- Green verification passed:
  - `rtk pnpm exec vitest run --project unit packages/training/src/climbing-grades.test.ts`
  - `rtk pnpm --filter @dofek/training typecheck` (`rtk` warned the filter is ignored for pnpm tsc; TypeScript still reported no errors).
- Starting Task 3: DB schema failing tests.
- Task 3 red tests added to `src/db/drizzle-schema.test.ts` and `scripts/migration-policy.test.ts`.
- Red verification passed:
  - `rtk pnpm exec vitest run --project unit src/db/drizzle-schema.test.ts` fails because `climbingEntry`, `climbingClimbTypeEnum`, and `climbingGradeSystemEnum` are undefined.
  - `rtk pnpm exec vitest run --project unit scripts/migration-policy.test.ts` fails because `drizzle/0047_climbing_entry.sql` does not exist.
- Starting Task 4: implement climbing entry schema and migration.
- Task 4 implemented:
  - `climbingClimbTypeEnum` and `climbingGradeSystemEnum` in `src/db/schema/enums.ts`.
  - `climbingEntry` in `src/db/schema/activity.ts`.
  - `drizzle/0047_climbing_entry.sql` with enums, table, checks, partial unique index, and query indexes.
- Green verification passed:
  - `rtk pnpm exec vitest run --project unit src/db/drizzle-schema.test.ts`
  - `rtk pnpm exec vitest run --project unit scripts/migration-policy.test.ts`
  - `rtk pnpm typecheck`
- Starting Task 5: repository failing tests.
- Task 5 red test added at `packages/server/src/repositories/climbing-repository.test.ts`.
- Red verification passed: `rtk pnpm exec vitest run --project unit packages/server/src/repositories/climbing-repository.test.ts` failed because `climbing-repository.ts` did not exist.
- Task 6 implemented `packages/server/src/repositories/climbing-repository.ts` with domain models, Zod row schemas, `executeWithSchema()`, and `fitness.v_activity` joins through `ANY(a.member_activity_ids)`.
- Green verification passed:
  - `rtk pnpm exec vitest run --project unit packages/server/src/repositories/climbing-repository.test.ts`
  - `rtk pnpm --filter dofek-server typecheck` (`rtk` warned the filter is ignored for pnpm tsc; TypeScript still reported no errors).
- Starting Task 7: router failing tests.
- Task 7 red tests added at `packages/server/src/routers/climbing.test.ts` and `packages/server/src/router.test.ts`.
- Red verification passed:
  - `rtk pnpm exec vitest run --project unit packages/server/src/routers/climbing.test.ts` fails because `packages/server/src/routers/climbing.ts` does not exist.
  - `rtk pnpm exec vitest run --project unit packages/server/src/router.test.ts` fails because `appRouter` does not include `climbing`.
- Starting Task 8: implement climbing router and public types.
- Task 8 implemented:
  - `packages/server/src/routers/climbing.ts` with `gradeProgression`, `volumeByGrade`, and `sessionSummary` cached protected queries.
  - `climbing` registration in `packages/server/src/router.ts`.
  - public type exports in `packages/server/src/types.ts`.
- Green verification passed:
  - `rtk pnpm exec vitest run --project unit packages/server/src/routers/climbing.test.ts`
  - `rtk pnpm exec vitest run --project unit packages/server/src/router.test.ts`
  - `rtk pnpm --filter dofek-server typecheck` (`rtk` warned the filter is ignored for pnpm tsc; TypeScript still reported no errors).
- Starting Task 9: Kaya parser contract failing tests.
- Task 9 fixture and red tests added:
  - `src/providers/kaya/fixtures/representative-export.csv`
  - `src/providers/kaya/import.test.ts`
- Red verification passed: `rtk pnpm exec vitest run --project unit src/providers/kaya/import.test.ts` failed because `src/providers/kaya/import.ts` does not exist.
- Starting Task 10: implement pure Kaya parser contract.
- Task 10 implemented `src/providers/kaya/import.ts` with pure parsing functions, RFC 4180 CSV decoding, Zod row validation, strict header validation, Kaya date parsing, grade normalization, bouldering inference from V-scale grades, one output entry per CSV row, stable external ids, and structured parse errors.
- Green verification passed:
  - `rtk pnpm exec vitest run --project unit src/providers/kaya/import.test.ts packages/training/src/climbing-grades.test.ts`
  - `rtk pnpm typecheck`
- Starting Task 11: queued Kaya import and upload-route failing tests.
- Task 11 red tests added:
  - `src/jobs/queues.test.ts` now includes `"kaya-export"` in the import job type contract.
  - `src/jobs/process-import-job.test.ts` expects the worker to read a Kaya CSV file, call `importKayaExportFile()`, log provider `"kaya-export"`, and emit a Kaya-specific completion message.
  - `packages/server/src/routes/upload.test.ts` expects authenticated `/kaya-export` upload and status routes with CSV-only validation.
- Red verification passed:
  - `rtk pnpm exec vitest run --project unit src/jobs/process-import-job.test.ts` fails because the worker does not yet handle `"kaya-export"`.
  - `rtk pnpm exec vitest run --project unit packages/server/src/routes/upload.test.ts` fails because `/api/upload/kaya-export` routes do not exist.
  - `rtk pnpm exec vitest run --project unit src/jobs/queues.test.ts` passes at runtime because the new assertion is type-level; `rtk pnpm typecheck` will enforce it once implementation starts.
- Starting Task 12: implement Kaya import job and upload route.
- Task 12 implemented:
  - Added `"kaya-export"` to `ImportJobData`.
  - Added the Kaya branch in `src/jobs/process-import-job.ts`.
  - Added `/api/upload/kaya-export` and `/api/upload/kaya-export/status/:jobId` in `packages/server/src/routes/upload.ts`.
  - Added `importKayaExportFile()` backed by an OOP `KayaExportImporter` that ensures the provider, upserts climbing sessions, replaces imported climbing entries, and returns a `SyncResult`.
- User clarified OOP preference applies to previous work too, not only future code.
- OOP refactor completed for earlier parser work:
  - `packages/training/src/climbing-grades.ts` now delegates to a `ClimbingGradeParser` class with native `#` private methods.
  - `src/providers/kaya/import.ts` now delegates pure CSV parsing to a `KayaExportParser` class while preserving the public `parseKayaExport()` API.
- Green verification passed:
  - `rtk pnpm exec vitest run --project unit packages/training/src/climbing-grades.test.ts src/providers/kaya/import.test.ts src/jobs/queues.test.ts src/jobs/process-import-job.test.ts packages/server/src/routes/upload.test.ts`
  - `rtk pnpm typecheck`
- Starting Task 13: provider import UI failing tests.
- Task 13 red test added to `packages/web/src/components/DataSourcesPanel.test.tsx`.
- Red verification passed:
  - `rtk pnpm exec vitest run --project unit packages/web/src/components/DataSourcesPanel.test.tsx` fails because Kaya renders as a sync provider card instead of a file-import zone.
- Task 14 implemented Kaya in `DataSourcesPanel` as a file-import config using `/api/upload/kaya-export` and `/api/upload/kaya-export/status`.
- Green verification passed:
  - `rtk pnpm exec vitest run --project unit packages/web/src/components/DataSourcesPanel.test.tsx`
  - `rtk pnpm --filter dofek-web typecheck` (`rtk` warned the filter is ignored for pnpm tsc; TypeScript still reported no errors).
- Starting Task 15: web climbing component failing tests.
- Task 15 red tests added:
  - `packages/web/src/components/ClimbingGradeProgressionChart.test.tsx`
  - `packages/web/src/components/ClimbingVolumeByGradeChart.test.tsx`
  - `packages/web/src/components/ClimbingSessionSummaryTable.test.tsx`
- Red verification passed:
  - `rtk pnpm exec vitest run --project unit packages/web/src/components/ClimbingGradeProgressionChart.test.tsx packages/web/src/components/ClimbingVolumeByGradeChart.test.tsx packages/web/src/components/ClimbingSessionSummaryTable.test.tsx` fails because the component files do not exist.
- Task 16 implemented:
  - `ClimbingGradeProgressionChart` with OOP chart model, existing `DofekChart`, visible boulder/route latest-grade summary, and Storybook states.
  - `ClimbingVolumeByGradeChart` with OOP chart model, existing `DofekChart`, totals, sorted grade cards, and Storybook states.
  - `ClimbingSessionSummaryTable` with OOP table model, existing `ActivityTable`, empty state, and Storybook states.
- Green verification passed:
  - `rtk pnpm exec vitest run --project unit packages/web/src/components/ClimbingGradeProgressionChart.test.tsx packages/web/src/components/ClimbingVolumeByGradeChart.test.tsx packages/web/src/components/ClimbingSessionSummaryTable.test.tsx`
  - `rtk pnpm --filter dofek-web typecheck` (`rtk` warned the filter is ignored for pnpm tsc; TypeScript still reported no errors).
- Starting Task 17: web climbing route failing tests.
- Task 17 red tests added:
  - `packages/web/src/routes/training/climbing.test.tsx`
  - `packages/web/src/routes/training.test.tsx`
- Red verification passed:
  - `rtk pnpm exec vitest run --project unit packages/web/src/routes/training/climbing.test.tsx packages/web/src/routes/training.test.tsx` fails because the climbing route file does not exist and the training tabs do not include `Climbing`.
- Task 18 implemented:
  - `packages/web/src/routes/training/climbing.tsx` with four sections: grade progression, volume by grade, recent climbing sessions, and recent climbing activities.
  - Added `Climbing` to `packages/web/src/routes/training.tsx`.
  - Ran `rtk pnpm exec tsr generate` from `packages/web`; the generated route tree now includes `/training/climbing`.
  - Added `"kaya-export"` to `providerIdForImportType()` mapping in `packages/server/src/routers/sync-helpers.ts` after web typecheck surfaced the missing import provider mapping.
- Green verification passed:
  - `rtk pnpm exec vitest run --project unit packages/web/src/routes/training/climbing.test.tsx packages/web/src/routes/training.test.tsx`
  - `cd packages/web && rtk pnpm typecheck`
- Starting Task 19: mobile climbing surface failing tests.
- Decided implementation shape: keep the existing mobile Training/Strain tab and add compact climbing sections inside it; no new bottom tab and no mobile file upload for this pass.
- Task 19 red tests added to `packages/mobile/app/(tabs)/strain.test.tsx` for populated climbing data, empty climbing states, and server error display.
- Red verification passed:
  - `rtk pnpm test:mobile -- 'packages/mobile/app/(tabs)/strain.test.tsx'` runs the mobile project and fails the new climbing assertions because the mobile screen does not render climbing data yet.
- Task 20 implemented:
  - Extended `mobileDashboard.training` server output with the same server-computed climbing grade progression, volume by grade, and session summary rows used by web.
  - Added a compact climbing panel inside `packages/mobile/app/(tabs)/strain.tsx`, backed by an OOP `ClimbingSectionModel`.
  - Updated existing mobile dashboard unit mocks for the new `climbing` payload.
- Green verification passed:
  - `rtk pnpm test:mobile -- 'packages/mobile/app/(tabs)/strain.test.tsx'`
  - `cd packages/mobile && rtk pnpm typecheck`
  - `rtk pnpm exec vitest run --project unit packages/server/src/routers/mobile-dashboard.test.ts`
  - `rtk pnpm typecheck`
- Starting Task 21: server integration coverage.
- Task 21 integration test added at `packages/server/src/routers/climbing.integration.test.ts`.
- Initial integration run failed before test execution:
  - Command: `rtk docker compose up -d db redis && rtk docker compose ps db redis && rtk pnpm exec vitest run --project integration packages/server/src/routers/climbing.integration.test.ts`
  - First fatal line from DB logs: `FATAL:  could not create directory "base/4": No space left on device`
  - Root cause: Docker storage was full enough that the workspace Postgres container could not initialize.
- Remediation: pruned unused Docker build cache only (`rtk docker builder prune -f`), recreated this workspace's Docker volumes (`rtk docker compose down -v && rtk docker compose up -d db redis`), and reran the integration suite.
- Green verification passed:
  - `rtk pnpm exec vitest run --project integration packages/server/src/routers/climbing.integration.test.ts`
- Final verification pass:
  - `rtk pnpm exec vitest run --project unit packages/training/src/climbing-grades.test.ts src/db/drizzle-schema.test.ts scripts/migration-policy.test.ts src/providers/kaya/import.test.ts src/jobs/queues.test.ts src/jobs/process-import-job.test.ts packages/server/src/repositories/climbing-repository.test.ts packages/server/src/routers/climbing.test.ts packages/server/src/router.test.ts packages/server/src/routers/mobile-dashboard.test.ts packages/web/src/components/DataSourcesPanel.test.tsx packages/web/src/components/ClimbingGradeProgressionChart.test.tsx packages/web/src/components/ClimbingVolumeByGradeChart.test.tsx packages/web/src/components/ClimbingSessionSummaryTable.test.tsx packages/web/src/routes/training/climbing.test.tsx packages/web/src/routes/training.test.tsx`
  - `rtk pnpm test:mobile -- 'packages/mobile/app/(tabs)/strain.test.tsx'`
  - `rtk pnpm typecheck`
  - `cd packages/web && rtk pnpm typecheck`
  - `cd packages/mobile && rtk pnpm typecheck`
  - `rtk env DBT_CLICKHOUSE_HOST=127.0.0.1 DBT_CLICKHOUSE_PORT=55126 DBT_CLICKHOUSE_USER=default DBT_CLICKHOUSE_PASSWORD=health pnpm lint`
- Lint note:
  - `pnpm lint` needed the locally mapped ClickHouse HTTP port because compose exposed `8123` on `127.0.0.1:55126` in this workspace.
  - Credentials came from `docker-compose.yml` and the running container: `DBT_CLICKHOUSE_USER=default`, `DBT_CLICKHOUSE_PASSWORD=health`.
