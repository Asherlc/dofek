{
  "scripts": {
    "setup": "corepack enable && pnpm install --frozen-lockfile && codegraph init -i",
    "run": "pnpm --filter dofek-web dev --host 127.0.0.1 --port \"$CONDUCTOR_PORT\""
  },
  "runScriptMode": "concurrent"
}
